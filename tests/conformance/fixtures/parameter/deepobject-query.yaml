# coverage: [objects.Parameter.style.deepObject, objects.Parameter.in.query]
# fails_at: L3
# reason: client/server wire behavior has focused tests; the harness does not yet execute L3
openapi: 3.1.0
info: { title: DeepObject Demo, version: 1.0.0 }
paths:
  /search:
    get:
      operationId: search
      parameters:
        - name: filter
          in: query
          style: deepObject
          explode: true
          schema:
            type: object
            properties:
              kind: { type: string }
              region: { type: string }
      responses:
        "200": { description: ok }
