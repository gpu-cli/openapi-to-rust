# coverage: [objects.Schema.$dynamicRef, objects.Schema.$dynamicAnchor]
# fails_at: L3
# reason: fields are typed, but full dynamic-anchor scope resolution remains unimplemented
openapi: 3.1.0
info: { title: $dynamicRef Demo, version: 1.0.0 }
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
components:
  schemas:
    Tree:
      $id: "/schemas/tree"
      $dynamicAnchor: node
      type: object
      properties:
        value: { type: integer }
        children:
          type: array
          items:
            $dynamicRef: "#node"
paths: {}
