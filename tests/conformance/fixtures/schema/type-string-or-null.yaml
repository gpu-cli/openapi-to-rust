# coverage: [objects.Schema.type[array]]
# fails_at: L3
# reason: 3.1 canonical nullability — Schema::Typed accepts only single SchemaType
openapi: 3.1.0
info: { title: Nullable Demo, version: 1.0.0 }
components:
  schemas:
    Thing:
      type: object
      properties:
        nickname:
          type: ["string", "null"]
      required: [nickname]
paths: {}
