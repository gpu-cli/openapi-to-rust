# coverage: [objects.Path Item.fields.query]
# reason: D1 — PathItem.query is typed; codegen emits a method that uses reqwest::Method::from_bytes("QUERY")
openapi: 3.2.0
info:
  title: Query Method Demo
  version: 1.0.0
paths:
  /search:
    query:
      operationId: search
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                filter: { type: string }
      responses:
        "200":
          description: results
          content:
            application/json:
              schema:
                type: array
                items: { type: string }
