# coverage: [objects.Components.fields.mediaTypes]
# fails_at: L3
# reason: field is typed; the conformance harness does not yet execute its L3 codegen layer
openapi: 3.2.0
info: { title: MediaTypes Component Demo, version: 1.0.0 }
components:
  mediaTypes:
    JsonThing:
      schema:
        $ref: "#/components/schemas/Thing"
  schemas:
    Thing:
      type: object
      properties:
        name: { type: string }
paths:
  /things:
    get:
      operationId: getThing
      responses:
        "200":
          description: ok
          content:
            application/json:
              $ref: "#/components/mediaTypes/JsonThing"
